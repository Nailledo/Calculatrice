<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Couleur de fond de la vue -->
    <solid android:color="#FFFFFF" />

    <!-- Paramètres de la bordure -->
    <stroke
        android:width="1dp"
        android:color="#000000" />

    <!-- Optionnel : pour arrondir les coins -->
    <corners android:radius="4dp" />

    <!-- Optionnel : pour ajouter de l'espace entre la bordure et le contenu -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>
    